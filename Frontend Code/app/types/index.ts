export type WhatsDueItem = {
    type: string;
    course: string;
    topic: string;
    date: string;
    buttonText: string;
};

export type AnnouncementItem = {
    sender: string;
    course: string;
    message: string;
    avatar: string;
};